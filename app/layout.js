export const metadata = {
  title: "Sameha Land 💗",
  description: "100 Days of Love",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
